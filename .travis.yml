language: node_js
node_js:
  - "8"
cache: yarn
script:
  - yarn build-data
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: webpage
  local_dir: build
